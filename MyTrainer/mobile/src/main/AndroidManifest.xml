<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ahisahar.mytrainer">

    <service android:name=".MainActivity">
        <intent-filter>
            <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />

            <data
                android:host="*"
                android:pathPrefix="/accelerometer0"
                android:scheme="wear" />
        </intent-filter>
    </service>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogInActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Power" />
        <activity android:name=".power_select_exercise" />
        <activity android:name=".Listener" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
        <service android:name=".MainActivity">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                    <data android:scheme="wear" android:host="*" android:pathPrefix="/accelerometer0" />
            </intent-filter>
        </service>
        -->

        <activity android:name=".deadLift" />
        <activity
            android:name=".deadlift_descriptor"
            android:label="@string/title_activity_deadlift_descriptor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".bench_press_descriptor" />
        <activity android:name=".squat_descriptor" />
        <activity android:name=".remo" />
        <activity android:name=".row_descriptor"></activity>
    </application>

</manifest>